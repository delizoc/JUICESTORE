<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="" name="keywords">
    <meta content="" name="description">  
    <title>Customers</title>
    <link rel="stylesheet" href="css/style.css">  
</head>

<body>
    <main>
        <div class="container">
            <br>
            <h4 class="title is-4"><strong>Add a New Customer</strong></h4>
            <form name="customer_info"  id="customer_info" class="inputborder">
                <label>First Name</label>
                <input type = "text" id = "custFirstName-input" class='input' placeholder="First Name" ></input>
                <br>
                <label>Last Name</label>
                <input type = "text" id = "custLastName-input" class='input' placeholder="Last Name" ></input>
                <br>
                <button name="add-cust-btn" id = "add-cust-btn" function="return view(event)" >Submit</button>
            </form>

        <br>
        <div class="row centered mt grid">
            <div class id="myTable"></div>
            <table id="myTable" class="table">  
                <thead>
                    <tr>
                        <th>customerID</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                    </tr>
                </thead>
                <tbody>
                {{#each customersResults.rows}}
                    <tr id=`customers-row-{{this.customerID}}`>
                        <form>
                            <td><input id=`customers-editCustomerID-{{this.customerID}}` class='input' type="number" value = {{this.customerID}} disabled></td>
                            <td><input id=`customers-editFirstName-{{this.customerID}}` class='input' type="text" value = '{{{this.fname}}}' disabled></td>
                            <td><input id=`customers-editLastName-{{this.customerID}}` class='input' type="text" value = '{{{this.lname}}}' disabled></td>
                            <td><button type = 'button' id=`customers-edit-{{this.customerID}}` onclick="updateCustomer('`customers-edit-{{this.customerID}}`', {{this.customerID}})" class='editButton_button'>Edit</button></td>
                            <td><button id=`customers-delete-{{this.customerID}}` onclick="deleteCustomer({{this.customerID}})" class='deleteButton_button'>Delete</button></td>
                        </form>
                    </tr>
                {{/each}}
                </tbody>
            </table>
        </div>
    </main>    
</body>
<script src = "/customers.js"></script>

</html>